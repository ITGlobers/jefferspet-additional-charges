query getProductAttachments(
  $identifier: ProductUniqueIdentifier
) {
  product(identifier: $identifier)
    {
    productName
    properties {
      originalName
      name
      values
    }
  }
}
